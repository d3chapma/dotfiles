function g {
  if [[ $# > 0 ]]; then
    git $@
  else
    git status
  fi
}

function gco() {
  if [[ $# > 0 ]]; then
    git checkout $@
  else
    git checkout $(git branch | fzf --height 20%)
  fi
}
